# PostHog Nginx Reverse Proxy - Environment Variables Example
# Copy this file to .env and adjust the values for your environment

# Server Configuration
SERVER_NAME=_
POSTHOG_CLOUD_REGION=us
PORT=8080

# CORS Configuration - Comma-separated list of allowed origins
# Examples:

# Single origin
# CORS_ALLOWED_ORIGINS=https://app.example.com

# Multiple origins
# CORS_ALLOWED_ORIGINS=https://app.example.com,https://staging.example.com,https://dev.example.com

# With regex patterns (wildcard subdomains)
# CORS_ALLOWED_ORIGINS=https://app.example.com,https://.*\.preview\.example\.com

# Development (localhost)
# CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000,https://localhost:3000

# Production example
CORS_ALLOWED_ORIGINS=https://app.turbodocx.com,https://staging.turbodocx.com
